{
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "$id": "https://example.com/product.schema.json",
 "title": "TouchdesignerConfig",
 "description": "A dynamic and schemabased jsonConfig for Touchdesigner",
 "type": "object",
 "properties": {
  "Global": {
   "description": "",
   "type": "object",
   "properties": {
    "TransportPort": {
     "description": "The port that is used for the transport using TouchIn/Out",
     "type": [
      "integer"
     ]
    }
   }
  },
  "Plugins": {
   "description": "",
   "type": "object",
   "properties": {
    "Ui": {
     "description": "Should Ui be activated for this deployment?",
     "type": [
      "boolean"
     ]
    },
    "AudioPlayer": {
     "description": "Should AudioPlayer be activated for this deployment?",
     "type": [
      "boolean"
     ]
    },
    "MoviePlayer": {
     "description": "Should MoviePlayer be activated for this deployment?",
     "type": [
      "boolean"
     ]
    },
    "VideoOutput": {
     "description": "Should VideoOutput be activated for this deployment?",
     "type": [
      "boolean"
     ]
    },
    "PlayerTransport": {
     "description": "Should PlayerTransport be activated for this deployment?",
     "type": [
      "boolean"
     ]
    }
   }
  }
 }
}